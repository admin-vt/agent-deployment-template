# Local env — populated from Doppler by scripts/worktree-setup.sh
.env
.env.*
!.env.example

# Doppler local binding
.doppler.yaml

node_modules/
.vercel/

# Orca session artifacts
.orca/

# Per-deployment Mastra CLI binding — never template state
.mastra-project.json

# Build artifacts
.mastra/
*.tsbuildinfo
src/mastra/public/dist/
.next/
