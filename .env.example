# Populated automatically from Doppler by scripts/worktree-setup.sh — do not fill by hand.
# Doppler project: agent-deployment-template (configs: dev / stg / prd)

# Mastra platform (CLI auth is token-based; headless)
MASTRA_API_TOKEN=
MASTRA_ORG_ID=
MASTRA_PROJECT_ID=

# Composio — the entire tool layer (catalog, per-user auth, Remote Sandbox)
COMPOSIO_API_KEY=

# OpenRouter — dev/test key only; end users bring their own (BYOK)
OPENROUTER_API_KEY=

# GitHub token the sandbox uses to clone/push the agent workspace repo
GITHUB_TOKEN=

# Firecrawl — operator credential behind the Composio firecrawl toolkit
FIRECRAWL_API_KEY=

# WorkOS AuthKit (onboarding surface)
WORKOS_API_KEY=
WORKOS_CLIENT_ID=
WORKOS_COOKIE_PASSWORD=
NEXT_PUBLIC_WORKOS_REDIRECT_URI=
